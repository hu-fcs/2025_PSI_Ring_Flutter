name: fluttersample_2025
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  sqflite: ^2.4.2
  mobile_scanner: ^3.0.0
  qr_flutter: ^4.1.0
  grpc: ^3.2.4
  protobuf: ^2.1.0  # 👈 ここを2.1.0にダウングレード

dev_dependencies:
  protoc_plugin: ^20.0.1  # 👈 ここも20.0.1にダウングレード
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  assets:
    - assets/empty_ecd.db
    - assets/mykey.db
    - assets/exchangekey.db
  uses-material-design: true
